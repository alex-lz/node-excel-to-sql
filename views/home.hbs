<html lang="en">
{{> head}}
<body>
    {{> navbar}}
    <div class="container">
		<h2>Convertir tabla de excel a consultas de sql</h2>
        <form 
		 ref='uploadForm' 
		 id='uploadForm' 
		 action='/upload' 
		 method='post' 
		 encType="multipart/form-data"
		>
		    <input type="file" name="sampleFile" accept=".xls, .xlsx" id="file" class="inputfile" onchange="preview_image()"/>
            <label for="file" id="fileName">Subir un archivo</label>
		    <input type='submit' value='Subir' id="upload" class="button button2" style="display: none;"/>
		</form>	
		<form method="get" action="/download">
            <button type="submit" class="button button3">Descargar</button>
        </form>
    </div>
    {{> footer}}
	<script>
        function preview_image() {
            var fullPath = document.getElementById('file').value;
			var etiqueta = document.getElementById('fileName');
			var btnUpload = document.getElementById('upload');
            if (fullPath) {
                var startIndex = (fullPath.indexOf('\\') >= 0 ? fullPath.lastIndexOf('\\') : fullPath.lastIndexOf('/'));
                var filename = fullPath.substring(startIndex);
                if (filename.indexOf('\\') === 0 || filename.indexOf('/') === 0) {
                    filename = filename.substring(1);
                }
                btnUpload.style.display = "inline";
				etiqueta.innerHTML = filename;
            }
        }
    </script>
</body>
</html>